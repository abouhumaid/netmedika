# NetMedika Web (Minimal React)

Simple Vite+React app for medicine orders with authentication, dashboard, and orders management.

Quick start:

1. Install dependencies

```bash
npm install
```

2. Run dev server

```bash
npm run dev
```

Notes:
- This project uses localStorage as a mock backend. Register a user, create or accept/reject orders in the UI.
